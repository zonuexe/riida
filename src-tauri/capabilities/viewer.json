{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "viewer",
  "description": "Capability for the standalone viewer windows spawned by open_viewer_window",
  "windows": ["viewer-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
